<Window x:Class="TurtlePig.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TurtlePig" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <TextBox  TextWrapping="Wrap" AcceptsReturn="True" Grid.Column="0" Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Name="code">
        </TextBox>
        <Canvas Grid.Column="2" Grid.Row="0" Name="canvas1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <GridSplitter ResizeDirection="Columns" Grid.Column="1" Width="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.RowSpan="2"/>
        <Button Grid.Column="0" Grid.Row="1" Click="Run">Run</Button>
    </Grid>
</Window>
